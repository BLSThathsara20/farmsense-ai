/**
 * @deprecated Import from '../api/mock/data' or use domain services from '../api' instead.
 * This re-export exists for backward compatibility during migration.
 */
export * from '../api/mock/data'
