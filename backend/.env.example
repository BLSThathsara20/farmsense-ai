DATABASE_URL=postgresql+psycopg://postgres:farmsense@localhost:5433/farmsense
JWT_SECRET=farmsense-dev-secret-change-in-production
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ENVIRONMENT=development
CACHE_TTL_SECONDS=60
OVERSUPPLY_THRESHOLD=100
FRONTEND_URL=http://localhost:5173

# Free email via Resend (https://resend.com — 100 emails/day free)
# 1. Sign up at resend.com
# 2. Create an API key
# 3. Paste it below (without key, reset links still work in development via API response)
RESEND_API_KEY=
EMAIL_FROM=FarmSense AI <onboarding@resend.dev>

# Market demand signals are FREE — no keys required:
# - Google Trends UK (unofficial public endpoints)
# - Wikimedia Wikipedia pageviews (fallback)
# Recommendation enrichment (also free, no keys):
# - Open-Meteo agri (ET₀ + soil moisture) blended into L2 weather
# - GOV.UK crop price indices for per-crop L3 scores
# Optional paid upgrades later: SerpAPI, official Google Trends API
